import chroma from 'chroma-js';

export class Colors {
    static readonly dimgray = chroma('dimgray').hex();
    static readonly dodgerblue = chroma('dodgerblue').hex();
    static readonly white = chroma('white').hex();
    static readonly black = chroma('black').hex();
    static readonly antiquewhite = chroma('antiquewhite').hex();
}
